<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Charity: Water – Change the World</title>
  <style>
    /* Brand Colors */
    :root {
      --yellow: #FFC907;
      --blue: #003366;
      --white: #ffffff;
      --gray: #f5f5f5;
    }

    body {
      margin: 0;
      font-family: 'Montserrat', 'Open Sans', sans-serif;
      color: var(--blue);
      background-color: var(--white);
      line-height: 1.6;
    }

    header {
      background: var(--blue);
      color: var(--white);
      padding: 1rem 2rem;
      text-align: center;
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
      font-weight: 700;
    }

    /* Hero Section */
    .hero {
      background: url("https://via.placeholder.com/1600x600") center/cover no-repeat;
      color: var(--white);
      text-align: center;
      padding: 5rem 2rem;
      position: relative;
    }

    .hero-overlay {
      background: rgba(0, 0, 0, 0.5);
      padding: 3rem;
      border-radius: 12px;
      display: inline-block;
    }

    .hero h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--yellow);
    }

    .hero p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
    }

    .cta-btn {
      background: var(--yellow);
      color: var(--blue);
      padding: 1rem 2rem;
      border: none;
      border-radius: 50px;
      font-size: 1.1rem;
      cursor: pointer;
      font-weight: bold;
      transition: 0.3s;
    }

    .cta-btn:hover {
      background: #e6b800;
    }

    /* About/Impact Section */
    .about {
      padding: 3rem 2rem;
      background: var(--gray);
      text-align: center;
    }

    .about h3 {
      font-size: 1.8rem;
      margin-bottom: 1rem;
    }

    .about p {
      max-width: 700px;
      margin: 0 auto;
    }

    /* Footer */
    footer {
      background: var(--blue);
      color: var(--white);
      text-align: center;
      padding: 1rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>charity: water</h1>
  </header>

  <section class="hero">
    <div class="hero-overlay">
      <h2>Change the World: One Drop at a Time</h2>
      <p>Join thousands of college students bringing clean, life-saving water to communities in need — with every dollar going directly to the cause.</p>
      <button class="cta-btn">Donate Now</button>
    </div>
  </section>

  <section class="about">
    <h3>Why College Students Give</h3>
    <p>College students want their passion to create real change. 100% of donations fund clean water projects, with visible, trackable results and a community of changemakers.</p>
  </section>

  <footer>
    <p>© 2025 charity: water | Made with ❤️ by students for change</p>
  </footer>
</body>
</html>
